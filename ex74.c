#include <stdio.h>
#include <math.h>
 int main()
{
       float p;
       printf("Enter the float digit");
       scanf("%f",&p);
       printf("round of  %f is  %f\n", p, round(p));
       return 0;
}
